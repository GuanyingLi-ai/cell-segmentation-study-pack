# Cell Segmentation 总框架

## 1. 问题定义

细胞分割的目标不仅是画出边界，还常包含三个相连任务：

1. **实例识别**：区分相互接触的不同细胞或细胞核。
2. **分子归属**：将空间转录本分配给其来源细胞。
3. **单细胞重建**：生成 cell × gene 表达矩阵及细胞形态、位置等元数据。

## 2. 输入与输出

| 数据类型 | 常见输入 | 常见输出 |
|---|---|---|
| 图像驱动 | DAPI、H&E、膜染色、IF/IMC 图像 | 标签掩膜、轮廓、多边形、质心 |
| 转录本驱动 | `(x, y[, z], gene)` 分子坐标 | transcript→cell 归属、细胞边界、表达矩阵 |
| 多模态/混合 | 图像、分子坐标、scRNA-seq 参考 | 边界、归属概率、细胞类型和表达矩阵 |
| 软分割 | 空间 bin/spot、表达与形态特征 | bin/分子对细胞的概率或权重、重建细胞 |

## 3. 方法分类

```text
Cell segmentation
├── A. Image-based
│   ├── Classical morphology: Watershed
│   ├── Shape-aware deep learning: StarDist
│   ├── Flow-field generalist: Cellpose
│   ├── Tissue whole-cell model: Mesmer
│   └── Foundation model: CellSAM
├── B. Transcript-informed / hybrid
│   ├── Probabilistic spatial model: Baysor
│   ├── Graph link prediction: Segger
│   └── Biologically informed self-supervision: BIDCell
├── C. Soft segmentation / reconstruction
│   └── SMURF
└── D. Analysis pipelines
    ├── Space Ranger (Visium/Visium HD)
    └── Sopa (cross-platform spatial omics)
```

说明：BIDCell 同时使用表达空间图、DAPI 和细胞类型参考，适合归为 hybrid；Sopa 与 Space Ranger 是工作流层级，不能与一个神经网络模型做完全等价的算法比较。

## 4. 各类方法的“分割哲学”

| 类别 | 基本假设 | 主要优势 | 典型风险 |
|---|---|---|---|
| 形态学 | 前景可阈值化，细胞可由种子与地形分开 | 快、透明、无训练 | 对噪声、参数和粘连敏感 |
| 形状模型 | 目标可由星凸多边形/多面体近似 | 粘连核分离好 | 非星凸、细长或复杂形态受限 |
| 流场模型 | 同一实例内像素流向共同中心 | 跨类型泛化较强 | 尺度、通道和域偏移影响明显 |
| 组织专用模型 | 大规模组织标注能学习核与全细胞边界 | 多重成像组织表现强 | 输入通道假设与训练域限制 |
| 基础模型 | 检测器产生提示，通用分割模型生成掩膜 | 多域、少调参潜力 | 显存、依赖和自动提示误差 |
| 转录本概率模型 | 同细胞分子在空间和表达组成上相似 | 无膜图像亦可工作 | 稀疏面板、密度变化、参数敏感 |
| 图模型 | transcript-cell 关系可视作边预测 | 可扩展、可融合 scRNA 先验 | 图构建和初始细胞候选影响结果 |
| 生物先验自监督 | 形态与细胞类型表达约束可共同定义边界 | 联合利用图像和表达 | 参考数据偏差、训练复杂 |
| 软分割 | 硬边界不足以表达归属不确定性 | 可保留混合与不确定性 | 输出解释和公平评估更复杂 |

## 5. 学习路线

### 阶段 1：建立分割基础

- 理解二值化、连通域、距离变换、marker-controlled watershed。
- 掌握 semantic segmentation 与 instance segmentation 的区别。
- 熟悉 Dice、IoU、Average Precision、AJI、PQ，以及过/欠分割。

### 阶段 2：图像深度学习

- StarDist：形状参数化与 NMS。
- Cellpose：流场预测、尺度和 mask dynamics。
- Mesmer：核/膜双通道、组织图像与后处理。
- CellSAM：检测提示与 SAM 掩膜生成。

### 阶段 3：空间转录组单细胞重建

- Baysor：空间混合模型、MRF 与先验分割。
- Segger：异质图、边预测、转录本归属。
- BIDCell：表达图、形态与细胞类型先验的自监督目标。
- SMURF：理解软归属与下游拓扑分析。

### 阶段 4：工程化管线

- Space Ranger：Visium HD 从 FASTQ/H&E 到 feature-cell matrix。
- Sopa：SpatialData、patching、分割器适配、聚合与报告。

## 6. 方法选型决策

| 场景 | 首选起点 | 备选/组合 |
|---|---|---|
| 清晰 DAPI、近圆形细胞核 | StarDist | Watershed、Cellpose |
| 多种显微成像、形态差异大 | Cellpose | CellSAM |
| 多重组织成像且有核+膜通道 | Mesmer | Cellpose |
| 希望跨域基础模型零/少样本使用 | CellSAM | Cellpose |
| 单分子空间转录组且边界染色弱 | Baysor | Segger |
| atlas 级转录本数据、关注速度与归属 | Segger | Baysor |
| 有 DAPI、空间表达和可靠 scRNA 参考 | BIDCell | Baysor/Segger |
| 希望表达归属不确定性、重建细胞 | SMURF | 与硬分割结果并行比较 |
| Visium HD 标准厂商流程 | Space Ranger | 导出后与替代分割比较 |
| 多平台、超大图像、统一工作流 | Sopa | 内部选择 Cellpose/Baysor/自定义分割器 |

## 7. 统一评估框架

### 图像层

- 像素：Dice、IoU。
- 实例：AP@IoU、PQ、AJI、匹配 F1。
- 错误类型：split、merge、miss、false positive、边界偏移。

### 分子归属层

- 分配率、背景率、核内/胞质分子比例。
- 邻近细胞 marker 污染、负 marker 泄漏。
- 每细胞基因数、UMI/转录本数及其空间分布。

### 生物层

- 已知 marker 的细胞类型特异性。
- scRNA 参考映射一致性。
- 细胞大小、形状、密度是否符合组织学预期。
- 下游聚类、空间邻域和差异表达对分割方案的敏感性。

### 工程层

- 运行时间、峰值内存/显存、磁盘、失败恢复、可重复性。
- ROI 到全切片的扩展规律；patch 边界伪影。

## 8. 最小 benchmark 设计

1. 选 3–5 个代表性 ROI：高密度、低密度、弱信号、形态特殊、组织边缘。
2. 固定坐标系与预处理；保存原始输入校验值。
3. 每个方法先跑官方默认，再做有限、预先声明的参数搜索。
4. 至少人工标注部分实例或转录本归属作为局部 gold standard。
5. 同时报告图像、分子、生物和工程指标。
6. 保存失败案例图，不仅保存均值表。

## 9. 已完成的 Baysor 最小复现

- 复现层级：**算法可执行性 / smoke reproduction**，不是论文全部真实数据 benchmark 的逐图重算。
- 固定环境：Baysor `v0.7.1`、Julia `1.10.7`、Apple Silicon macOS；随机种子由 Baysor CLI 固定为 `1`，合成输入种子为 `202208`。
- 输入：60 个合成细胞、6 个基因、5,400 个转录本（其中 600 个背景转录本）；不使用先验分割。
- 参数：`scale=6.5`、`scale_std=25%`、`n_clusters=3`、`min_molecules_per_cell=20`、`iters=500`。
- 结果：推断 60 个细胞；全部分子分配率 94.98%；真实细胞分子的分配率 99.69%；ARI 0.994；加权 cell purity 99.87%。
- 结论：输入→概率分割→transcript assignment→cell × gene→polygon→QC 的主链路已跑通。高分仅说明模型能恢复按其假设构造的可分合成数据，不能外推为真实组织精度。
- 入口：[Baysor 复现说明](scripts/baysor/README.md)；[完整复现记录](records/Baysor_Petukhov_复现.md)。

## 9. StarDist 复现基线（2026-08-07）

本包已完成 StarDist 2D 的论文同源预训练推理复现：官方 `2D_paper_dsb2018` 模型在官方 DSB2018 test split 的 50 张图、2,460 个真值实例上运行，使用模型自带 probability/NMS 阈值和官方 matching 评价。在 IoU=0.50 时得到 Precision 0.9322、Recall 0.9228、F1 0.9275、PQ 0.7628；完整 mask、QC、日志、环境与逐图指标见 [复现记录](records/StarDist_2D_论文同源数据复现.md)。

这个结果在框架中的角色是“清晰核图像、近星凸对象”的可追溯强基线。它验证了 2D 检测/分割路径，不代表重新训练了论文模型。

本包随后完成了 [StarDist 3D 官方合成样例复现](records/StarDist_3D_官方合成样例复现.md)：`3D_demo` 模型在 3 个 `(64,128,128)` test volumes、235 个真值实例上得到 F1@0.50=0.9979、PQ@0.50=0.8160。该结果验证了星凸多面体、3D NMS、体素 mask 和体积评价链路，但合成样例不能代表真实各向异性显微体数据或论文完整 benchmark。

## 10. Watershed 复现基线（2026-08-07）

本包已在与 StarDist 相同的官方 DSB2018 test split 上完成 marker-controlled Watershed：Gaussian 平滑 → Otsu 前景 → 形态学清理 → 欧氏距离变换 → 局部极大值 markers → 对负距离图执行 watershed。50 张图、2,460 个真值实例上，IoU=0.50 的 Precision 为 0.7413、Recall 为 0.7512、F1 为 0.7462、PQ 为 0.5877；平均核心分割时间为 0.0231 s/图。完整配置、中间图、mask、QC、日志与逐图指标见 [复现记录](records/Watershed_DSB2018_复现.md)。

这一结果在框架中充当“无训练、低成本、可解释”的下界基线。与同数据 StarDist 的 F1 0.9275、PQ 0.7628 相比，它更快但对阈值、尺度和 marker 规则更敏感。公平调参时，应在 validation split 上选择前景和 marker 参数，再锁定配置评价 test split。
