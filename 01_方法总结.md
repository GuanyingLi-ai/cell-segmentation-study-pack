# Cell Segmentation 方法总结

## 1. Watershed

**定位**：经典数学形态学实例分割。

**简介与核心思想**：把灰度图或距离图看作地形，从标记点开始“注水”；不同盆地相遇处形成分水岭。细胞任务中常先阈值化得到前景，再对距离变换取局部极大值作为 marker，用 marker-controlled watershed 拆开粘连目标。

**输入**：DAPI/核图像、二值前景或梯度/距离图；可选 markers。

**输出**：整数标签掩膜、连通实例及边界。

**优势/局限**：无训练、速度快、可解释；但容易因噪声过分割，marker 不足则欠分割，对平滑、阈值、最小距离高度敏感。

**复现要点**：记录阈值算法、平滑核、形态学操作、距离度量、局部峰最小间距、连通性、边界对象处理。先可视化中间产物；禁止只保存最终 mask。

## 2. StarDist

**定位**：以星凸形状参数化为核心的 2D/3D 实例分割。

**简介与核心思想**：网络为像素预测对象概率和沿固定方向射线到边界的距离，由此形成星凸多边形/多面体；随后用非极大值抑制去除重复实例。特别适合相互接触、近星凸的细胞核。

**输入**：通常为归一化的单通道荧光核图；也有组织病理和 3D 模型。

**输出**：实例标签图、多边形/多面体、对象概率。

**优势/局限**：粘连核分离清晰、预训练模型和 Fiji/Python 生态成熟；复杂凹形、细长分枝细胞不符合星凸假设。

**复现要点**：确认 2D/3D 模型、像素尺寸、归一化百分位、tile overlap、probability/NMS threshold、射线数和训练 patch 尺寸。微调时保持 train/val/test 按样本而非 patch 隔离。

## 3. Cellpose

**定位**：通用流场式细胞实例分割。

**简介与核心思想**：网络预测细胞概率及每个像素的二维/三维流向；像素沿流场迭代汇聚，同一汇聚区域形成一个实例。通用模型从多样细胞形态中学习，适合无专用模型时作为强基线。

**输入**：单/双通道显微图像；常见为细胞质/膜通道加可选核通道。

**输出**：实例 mask、flow、cell probability 和可选样式/质量信息。

**优势/局限**：跨显微类型泛化较好、GUI/CLI/Python 易用；结果仍受图像尺度、通道指定、细胞直径和域偏移影响。

**复现要点**：固定软件大版本和模型名；记录 channels、diameter/scale、flow threshold、cell probability threshold、3D/2D 模式、tile 与 GPU。抽查 flow 失败与断裂/合并实例；不要把 Cellpose 与较新的 Cellpose-SAM 混记为同一模型。

## 4. Mesmer

**定位**：DeepCell 生态中的组织图像核与全细胞分割模型。

**简介与核心思想**：使用大规模 TissueNet 标注训练深度网络，结合核与细胞边界相关输出及专门后处理，在多重成像组织上实现全细胞分割。

**输入**：典型为两通道图像：核通道与膜/细胞质通道；需给出图像物理分辨率。

**输出**：whole-cell 或 nuclear 实例标签掩膜。

**优势/局限**：组织、多平台 multiplex imaging 场景强；通道质量、分辨率和训练组织域会影响迁移，通常不直接使用转录本坐标。

**复现要点**：保留核/膜通道构造规则、mpp（microns per pixel）、compartment、batch/tile、模型版本、后处理阈值。确认通道顺序和强度动态范围；全切片检查 tile seams。

## 5. CellSAM

**定位**：面向多类细胞影像的基础模型式实例分割。

**简介与核心思想**：在 Segment Anything 基础上，以 CellFinder 自动检测细胞并产生 prompts，再由 SAM 风格掩膜模型生成实例。目标是在组织、培养细胞、酵母、细菌和多种成像模态间泛化。

**输入**：细胞显微图像；自动流程由检测器产生 box prompts，也可围绕提示式标注使用。

**输出**：细胞实例掩膜及检测结果。

**优势/局限**：覆盖多域、可用于 human-in-the-loop 标注；推理依赖较重，自动检测漏检会传递至分割，显存和版本兼容需提前验证。

**复现要点**：优先复现预训练推理而非论文级训练；锁定 PyTorch/CUDA、checkpoint、图像通道转换、尺寸缩放、检测阈值、mask 后处理与重叠处理。分别评估 detector recall 与 conditional mask quality。

## 6. Baysor

**定位**：单分子空间转录组的概率式细胞分割。

**简介与核心思想**：联合建模转录本空间位置、基因组成与细胞形态，以混合模型/MRF 思路推断分子属于细胞或背景；可仅靠转录本，也能使用核/先验分割辅助。

**输入**：分子表（x、y、可选 z、gene）；可选 prior segmentation、核/染色信息。

**输出**：transcript→cell 分配、背景概率、细胞多边形/中心和 cell × gene 矩阵。

**优势/局限**：弱膜图像下可利用表达与空间结构；对分子密度、基因 panel、期望细胞大小和组织异质性敏感，计算量需关注。

**复现要点**：先统一坐标单位；记录 scale/细胞半径、prior 权重、最小分子数、背景模型、线程和随机种子。检查分子分配率、细胞大小、marker 污染；大数据先 ROI 调参再分块。

**本包状态**：已用 Baysor `v0.7.1` + Julia `1.10.7` 完成无先验合成点云的最小运行，产出 transcript assignment、cell × gene TSV、cell statistics、GeoJSON polygons 与 QC。该实验验证软件与算法主链路，不替代 ISS、osmFISH、MERFISH、STARmap 等真实数据 benchmark；详见 [复现记录](records/Baysor_Petukhov_复现.md)、[方法学习笔记](notes/Baysor_方法学习笔记.md)。

## 7. Segger

**定位**：以异质图神经网络进行 transcript-to-cell link prediction。

**简介与核心思想**：把转录本与细胞候选表示为异质图，将分割重述为转录本到细胞的链接预测；可利用 scRNA-seq 信息提升归属，并强调 atlas-scale 扩展性。

**输入**：单分子转录本坐标与基因身份、细胞/核候选；可选 scRNA-seq 参考。

**输出**：转录本-细胞链接、细胞分割/归属和表达矩阵。

**优势/局限**：速度和大规模处理是重点，能融合分子关系；图邻域、候选细胞和训练/模型版本改变会影响结果。

**复现要点**：使用当前稳定仓库与匹配文档；记录图构建半径/kNN、节点/边特征、初始核来源、模型 checkpoint、batch 和阈值。分别审计未分配分子与跨邻居误连。

## 8. BIDCell

**定位**：生物学先验驱动的自监督多模态 SST 分割。

**简介与核心思想**：深度模型结合空间基因表达图、DAPI 形态和参考细胞类型平均表达，通过正/负 marker 等生物学信息构造损失，使学习到的边界同时满足形态和表达一致性。

**输入**：空间基因表达 maps、对应 DAPI、scRNA-seq/细胞类型平均表达参考及 marker 信息。

**输出**：细胞分割 mask、细胞表达谱和相关 QC 结果。

**优势/局限**：能得到多样形态并降低表达污染；准备流程、训练成本和参考选择较复杂，错误参考可能把偏差写入边界。

**复现要点**：严格记录表达图生成、基因筛选、图像配准、参考数据处理、marker 规则、loss 权重、初始化和训练停止标准。必须做去除某类先验的消融，并检测参考与组织不匹配。

## 9. SMURF

**定位**：空间转录组的软分割、单细胞重建与拓扑分析。

**简介与核心思想**：不把每个空间单元强制硬分给唯一细胞，而是估计软归属/混合关系，以此重建单细胞表达并支持组织拓扑分析；重点是对边界不确定性和混合信号的表达。

**输入**：空间转录组表达、空间位置以及方法要求的形态/邻接信息。

**输出**：空间单元到细胞的权重或概率、重建的单细胞表达与拓扑分析结果。

**优势/局限**：适合边界模糊和混合观测，避免过早硬判定；与硬实例 mask 的评价口径不同，阈值化后可能丢失其优势。

**复现要点**：以论文和官方实现的实际输入 schema 为准；保存完整软分配矩阵，记录候选细胞数、空间邻域、正则项、收敛条件和后续硬化规则。分别评价重建表达与几何边界。

**学习笔记**：[SMURF_原始笔记.pdf](notes/SMURF_原始笔记.pdf)

## 10. Space Ranger

**定位**：10x Genomics Visium 数据的厂商端到端分析套件；不是单一分割算法。

**简介与核心思想**：对 FASTQ、参考基因组和显微图像进行配准、计数及下游分析。当前 Visium HD H&E 细胞分割流程先用定制 StarDist 分割细胞核，再把周围 HD squares 在最大扩张距离内分配给最近核，构建 feature-cell matrix。

**输入**：Visium/Visium HD FASTQ、参考、slide image 与 assay 元数据；`segment` 可仅对 H&E 运行核分割。

**输出**：配准结果、bin/feature matrices、核/细胞边界、feature-cell matrix、网页报告等。

**优势/局限**：标准化、与 10x 数据无缝衔接；算法与支持范围随版本变化，最近核扩张不等于真实膜边界，远离核的细胞质可能未分配。

**复现要点**：记录 Space Ranger 精确版本、参考 bundle、命令、图像像素尺寸、配准方式、nucleus expansion distance、资源配置和输出 schema。不要把 v3.x 与 v4.x 的能力混用；保存 web summary 与 pipeline metadata。

## 11. Sopa

**定位**：基于 SpatialData 的跨平台 image-based spatial omics 分析管线；分割是其模块之一。

**简介与核心思想**：把不同平台数据统一到 SpatialData，切为重叠 patches，调用 Cellpose、Baysor 或自定义分割器，再合并重复边界，聚合通道/转录本，完成注释、空间分析和报告。

**输入**：Xenium、MERSCOPE、CosMx、PhenoCycler 等平台的图像、转录本和元数据。

**输出**：SpatialData、细胞 polygons、聚合表达/强度、注释、可视化文件和 HTML QC 报告。

**优势/局限**：跨技术、内存友好、CLI/API/Snakemake 完整；最终质量仍取决于内部 segmenter，patch/merge 参数会产生工程性伪影。

**复现要点**：记录 Sopa/SpatialData 版本、reader、坐标变换、patch 大小与 overlap、segmenter 及其全部参数、merge 规则、聚合边界规则。验证 patch seams、重复/截断细胞和坐标往返一致性。

**学习笔记**：[SOPA 原始笔记.pdf](notes/SOPA_原始笔记.pdf)。**本包状态**：Human Breast Xenium DAPI/L2 tissue ROI 已成功复现（参考细胞质心覆盖 99.2%，ROI 面积 5.05%；[记录](records/SOPA_组织分割复现.md)）；完整细胞实例分割与 transcript aggregation 尚需独立评价。

## 快速对照表

| 方法 | 层级 | 主要模态 | 是否训练模型 | 典型输出 |
|---|---|---|---|---|
| Watershed | 算法 | 图像 | 否 | 实例 mask |
| StarDist | 模型 | 图像 | 是 | 星凸实例 mask |
| Cellpose | 模型 | 图像 | 是 | flow + 实例 mask |
| Mesmer | 模型/应用 | 核+膜图像 | 是 | 核/全细胞 mask |
| CellSAM | 基础模型 | 图像 | 是 | 检测+实例 mask |
| Baysor | 算法 | 转录本±先验 | 概率推断 | 分子归属+细胞 |
| Segger | 模型 | 转录本+候选细胞 | 是 | 图链接+细胞 |
| BIDCell | 模型 | 表达图+DAPI+参考 | 是/自监督 | mask+表达 |
| SMURF | 模型/框架 | 空间表达 | 依实现 | 软归属+重建细胞 |
| Space Ranger | 厂商管线 | Visium 数据+图像 | 内置 | 全流程结果 |
| Sopa | 通用管线 | 空间组学多模态 | 调用其他模型 | 统一数据与报告 |
